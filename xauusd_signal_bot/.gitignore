# secrets - never commit
.env

# generated data - top level and every per-market subdirectory
data/*.csv
data/*.csv.bak-*
data/*.txt
data/*.json
data/*/
!data/.gitkeep

# downloaded / exported price history
history/*.csv

# RL artefacts: raw MT5 history, built datasets, trained models and reports.
# The model REGISTRY is tracked so model provenance survives; the weights and
# the multi-GB raw candles are not.
data/raw/
data/processed/
data/ai/
models/ppo/
reports/
!models/model_registry.json

# python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
